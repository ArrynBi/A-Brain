# library

`library` 保存外部资料、原始证据、导入内容和项目资料。它不是稳定知识层，而是 `ingest` 和 `think` 可以引用的资料库。

## Directories

```text
sources   原始资料、网页、PDF 转写、研究材料
projects  项目级原始文档或导入包
imports   会话、导出文件和一次性导入批次
```

资料进入 `library` 后，可以通过 `ingest` 编译为 `knowledge/notes` 或 source-driven concept candidates。
